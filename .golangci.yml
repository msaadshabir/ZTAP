run:
  timeout: 5m
  skip-dirs:
    - examples
  skip-files: []

issues:
  exclude-dirs:
    - examples

linters:
  enable-all: false
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
