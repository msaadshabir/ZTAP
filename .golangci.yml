run:
  timeout: 5m

issues:
  exclude-dirs:
    - examples

linters:
  disable:
    - typecheck # Already covered by go vet step in CI
    - govet # go vet runs separately within workflow
  enable:
    - errcheck
    - gosimple
    - ineffassign
    - staticcheck
    - unused
